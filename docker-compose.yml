services:
  flowise:
    image: flowiseai/flowise:latest
    container_name: flowise
    ports:
      - "3000:3000"
    volumes:
      - flowise-data:/root/.flowise
    restart: unless-stopped

volumes:
  flowise-data:
